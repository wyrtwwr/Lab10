# Домашнее задание к работе 10
## Условие задачи

Составьте программу, которая для заданной пользователем фигуры, например прямоугольника (треугольника или другой фигуры см. инидивидуальное задание работы 9) предлагает меню выбора одной из операций:
1) рассчитать площадь;
2) вывести определение фигуры;
3) нарисовать фигуру.

## 1. Алгоритм и блок-схема
## Алгоритм
1. Начало программы
2. Ввод данных пользователем:
   - `h` — высота вписанной окружности
3. Показать меню действий:
   1. Рассчитать площадь — вызвать функцию S(r, a)
   2. Вывести определение фигуры
   3. Нарисовать треугольник — вызвать draw_deltoid(r)
   4. Выйти из программы
4. Спросить, хочет ли пользователь продолжить
5. Если нет — завершить программу
6. Конец программы
   
### Блок-схема
![hhhh](https://github.com/user-attachments/assets/fba05c5d-6b89-49d0-829d-99d2c9072c86)


## 2. Реализация программы

```
#define _CRT_SECURE_NO_WARNINGS
#define _USE_MATH_DEFINES
#include <locale.h>
#include <stdio.h>
#include <stdlib.h>
#include <conio.h>
#include <math.h>


// Площадь равностороннего треугольника через высоту
float S_triangle(float h) {
    float a = 2 * h / sqrt(3);
    return (sqrt(3) / 4) * a * a;
}

// Контур треугольника (по умолчанию)
void draw_triangle_outline(int h) {
    for (int i = 1; i <= h; i++) {

        for (int j = 1; j <= h - i; j++)
            printf(" ");

        for (int j = 1; j <= 2 * i - 1; j++) {
            if (j == 1 || j == 2 * i - 1 || i == h)
                printf("*");
            else
                printf(" ");
        }
        printf("\n");
    }
}

int main() {
    setlocale(LC_ALL, "RUS");

    int choice;
    int h;

    while (1) {
        printf("\nВведите высоту равностороннего треугольника (h): ");
        scanf("%d", &h);

        printf("\n=== МЕНЮ ===\n");
        printf("1. Рассчитать площадь\n");
        printf("2. Вывести определение фигуры\n");
        printf("3. Нарисовать фигуру (контур)\n");

        printf("Выберите пункт меню: ");
        scanf("%d", &choice);

        switch (choice) {
        case 1:
            printf("Площадь равностороннего треугольника: S = %.2f\n", S_triangle(h));
            break;

        case 2:
            printf("Равносторонний треугольник — это треугольник, у которого все стороны равны.\n");
            break;

        case 3:
            printf("\nКонтур равностороннего треугольника:\n\n");
            draw_triangle_outline(h);
            break;

        default:
            printf("Неверный выбор. Попробуйте снова.\n");
        }

        printf("\nПродолжить? (y/n): ");
        char cont;
        scanf(" %c", &cont);
        if (cont == 'n' || cont == 'N') break;
    }

    return 0;
}
```


## 3. Результаты работы программы

```
Введите высоту равностороннего треугольника (h): 10

=== МЕНЮ ===
1. Рассчитать площадь
2. Вывести определение фигуры
3. Нарисовать фигуру (контур)
Выберите пункт меню: 1
Площадь равностороннего треугольника: S = 57,74

Продолжить? (y/n): y

Введите высоту равностороннего треугольника (h): 5

=== МЕНЮ ===
1. Рассчитать площадь
2. Вывести определение фигуры
3. Нарисовать фигуру (контур)
Выберите пункт меню: 1
Площадь равностороннего треугольника: S = 14,43

Продолжить? (y/n): y

Введите высоту равностороннего треугольника (h): 4

=== МЕНЮ ===
1. Рассчитать площадь
2. Вывести определение фигуры
3. Нарисовать фигуру (контур)
Выберите пункт меню: 2
Равносторонний треугольник - это треугольник, у которого все стороны равны.

Продолжить? (y/n):
```


<img width="1639" height="995" alt="{4A400CAE-024B-4AA2-937E-A3A1AE7D19EC}" src="https://github.com/user-attachments/assets/2ac0e785-5862-4661-a318-cdafa06f6dc5" />

